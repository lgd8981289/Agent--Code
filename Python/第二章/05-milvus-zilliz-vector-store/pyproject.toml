[project]
name = "agent-course-milvus-python"
version = "0.1.0"
description = "Python implementation of the Agent course Milvus RAG example"
requires-python = ">=3.11"
dependencies = [
    "pymilvus==2.6.12",
]
