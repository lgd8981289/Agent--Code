# 简化版 Tokenizer

这个示例使用一个很小的词表，演示文本被切分成 Token 的过程。

它采用“最长优先匹配”：从左到右扫描文本，每次优先匹配当前位置能找到的最长 Token。如果没有任何词表内容能够匹配，就把当前 Unicode 字符作为未知 Token。

## 运行

```bash
python3 toy_tokenizer.py
```

程序会依次输出三组文本的切分结果，预期 Token 数量分别为 `3`、`4` 和 `9`。

代码只使用 Python 标准库，不需要安装依赖。
