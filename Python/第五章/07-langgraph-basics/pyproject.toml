[project]
name = "agent-course-langgraph-basics-python"
version = "0.1.0"
description = "LangGraph Python State, Node, Edge and Conditional Edge basics"
requires-python = ">=3.11"
dependencies = [
    "langgraph==1.2.10",
    "pydantic==2.13.4",
]
