[project]
name = "agent-course-langchain-middleware-python"
version = "0.1.0"
description = "LangChain Python middleware permissions, budget, validation and retry example"
requires-python = ">=3.11"
dependencies = [
    "langchain==1.3.15",
    "langchain-deepseek==1.1.0",
    "langgraph==1.2.11",
    "pydantic==2.13.4",
]

