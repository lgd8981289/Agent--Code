services:
  postgres:
    image: postgres:17-alpine
    container_name: agent-memory-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: agent_memory
      POSTGRES_USER: agent_course
      POSTGRES_PASSWORD: agent_course
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agent_course -d agent_memory"]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - agent-memory-postgres-data:/var/lib/postgresql/data

volumes:
  agent-memory-postgres-data:
