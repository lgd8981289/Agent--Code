[project]
name = "agent-course-deep-agents-intro-python"
version = "0.1.0"
description = "Deep Agents introduction: write and verify a game world document"
requires-python = ">=3.11"
dependencies = [
    "deepagents==0.7.19",
    "langchain-deepseek==1.1.1",
]

[tool.uv]
package = false
