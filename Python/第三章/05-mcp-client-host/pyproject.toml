[project]
name = "agent-course-mcp-client-host"
version = "0.1.0"
description = "Python implementation of the MCP client host lesson"
requires-python = ">=3.11"
dependencies = [
    "mcp==1.28.1",
]

